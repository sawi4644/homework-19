/* this page should contain an import of the utils function so you can uise the fetch function you're writing, as well as the Employee.js component */


/*then there should be a map function that will create a series of employee.ls components*/

import React, { Component } from 'react';
import 

export default class all extends Component {
    render() {
        return (
            <div>
                {}
            </div>
        )
    }
}
